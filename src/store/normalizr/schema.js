import { schema } from 'normalizr';

export const floorplanSchema = new schema.Entity('floorplans');
export const deviceSchema = new schema.Entity('devices');
export const fixtureSchema = new schema.Entity('fixtures');
export const projectSchema = new schema.Entity('projects');
